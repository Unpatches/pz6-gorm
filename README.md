# pz6-gorm
Задачи проекта
1) Понять, что такое ORM и чем удобен GORM.
2) Научиться описывать модели Go-структурами и автоматически создавать таблицы (миграции через AutoMigrate).
3) Освоить базовые связи: 1:N и M:N + выборки с Preload.
4) Написать короткий REST (2–3 ручки) для проверки результата
---

## Установка и запуск

(Необходимы предустановленные Go, Git и PostgreSQL)

Клонировать репозиторий:

```
git clone https://github.com/Unpatches/pz6-gorm
cd pz6-gorm
```

Команда запуска сервера:

```
go run ./cmd/server
```


------

## Структура проекта

```plaintext
pz6-gorm/                     
├── cmd/                  
│   └── server/             
│       └── main.go       
├── internal/
│   └── db/
│   │   └── postgres.go       
│   └── httpapi/               
│   │   └── handlers.go     
│   │   └── router.go
│   └── models/
│       └── models.go       
├── go.mod                
└── go.sum            
```
## Описание
ORM (Object–Relational Mapping) — это технология сопоставления объектно-ориентированных сущностей программы (например, структур в Go) с таблицами реляционной базы данных.
Зачем он нужен: 
1) Ускорение разработки
2) Безопасность
3) Поддержка миграций
4) Кросс-СУБД

GORM — это самая популярная ORM-библиотека на языке Go.
Основные возможности:
1)	автоматическая генерация таблиц по структурам (AutoMigrate);
2)	CRUD-операции: Create, First, Find, Save, Delete;
3)	фильтрация и сортировка (Where, Order, Limit);
4)	связи 1:1, 1:N, M:N (has one, has many, many to many);
5)	автоматическая подгрузка связанных данных (Preload);
6)	работа с транзакциями;
7)	хуки (функции, вызываемые до/после операций).


## Запуск и проверка
<img width="666" height="603" alt="image" src="https://github.com/user-attachments/assets/027ba0a4-9861-44f3-8168-f6efbad0516c" />
